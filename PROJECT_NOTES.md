# Рабочие заметки по проекту psa-landing

Служебный файл для себя (агента) — то, что не выведешь просто чтением кода: история решений, тупики, грабли. `README.md` — для людей.

**Важно про этот файл:** он живёт только в рабочей копии, не в git (см. «Git и паттерн отката» ниже). Если в начале следующей сессии увидишь, что этого файла нет или он старый — не удивляйся, перечитай проект заново, не доверяй памяти из истории чата.

Последняя проверка: **2026-09-24**. С этого момента проект — **Django-монолит**, не статический сайт (см. «Django-монолит» ниже) — если в начале следующей сессии видишь отдельные `index.html`/`styles.css`/`script.js` в корне `psa-landing/` рядом с `backend/`, это откат/регрессия, а не текущее целевое состояние — стоит явно спросить у пользователя, прежде чем что-то чинить.

## Текущее фактическое состояние

`psa-landing/` содержит только `README.md`, `PROJECT_NOTES.md`, `.gitignore`, `.vercel/` (устарел, см. «Vercel» ниже) и `backend/` — весь сайт (Django-проект). Никакого отдельного статик-сервера для фронтенда больше нет.

**Пять страниц** (снова, но теперь правильно — через `{% extends "base.html" %}`, не копипастой шапки в каждый файл):
- `/` (`content.views.home` → `index.html`): Header → Hero → О компании → Проекты (3 карточки + модалки на `<dialog>`) → Партнёры (7 карточек-ссылок) → Контакты (форма с валидацией, клиентская, никуда не отправляется).
- `/local-content/`, `/procurement/`, `/documents/`, `/news/` — по одной секции на страницу, контент из БД (`{% for entry in ...entries %}`), у каждой своя вьюха-функция в `content/views.py`.

Все 5 расширяют `backend/templates/base.html` (шапка/меню/модалка проектов один раз, не дублируется).

**Язык по умолчанию — казахский, выбор сохраняется между страницами** (`localStorage`, ключ `psa-lang`). Добавлено 2026-09-24 после жалобы «переключаюсь по разделам — язык сбрасывается на казахский»: у каждой из 5 страниц свой полный HTML (не SPA), поэтому JS-переменная `currentLang` раньше просто обнулялась на дефолт при каждом переходе. Теперь `applyLanguage()` пишет выбор в `localStorage` при каждом вызове, а `currentLang` при инициализации читает оттуда (`translations[storedLang] ? storedLang : "kz"`). Новый посетитель без сохранённого выбора по-прежнему видит казахский. Обёрнуто в try/catch (приватный режим браузера может блокировать localStorage). Клиентский JS-переключатель (`translations` в `script.js`) как таковой не менялся при переходе на Django — i18n остаётся на клиенте, не мигрировала на Django-i18n.

Пользователь предлагал HTMX для этой задачи — сознательно не стал: HTMX (частичные AJAX-переходы вместо полной перезагрузки) решило бы проблему только для кликов по меню, но не для прямого перехода по ссылке/обновления страницы (F5) — там HTMX не участвует, полная перезагрузка всё равно случится. `localStorage` решает во всех случаях сразу и без новых зависимостей. Если понадобится более плавная навигация без перезагрузки (отдельная, самостоятельная задача, не связанная с этим багом) — можно добавить `hx-boost` на `<body>` в `base.html`, тогда все обычные ссылки полетят через htmx автоматически.

**Переводы неполные**: у `kz` и `en` блоков в `script.js` нет ключей `partner.*` (только `partners.eyebrow`/`partners.title`) — откат `translate()` на `ru`, так и задумано, не баг.

**Мёртвый-якорь баг в меню — исправлен окончательно.** Был описан здесь раньше в двух вариантах: сначала «секции на той же странице с реальными id», затем (по прямой просьбе «раздели на отдельные страницы») — 4 реальных URL. Нюанс, который решал в этом заходе: пункты меню «О компании»/«Проекты»/«Контакты» — это якоря на разделы ГЛАВНОЙ (`{% url 'home' %}#about` → реальный href `/#about`), а «Местное содержание» и т.д. — уже полноценные страницы. Плавный скролл в `script.js` раньше матчился по `a[href^="#"]` (чистый якорь) — с href вида `/#about` это перестало бы работать. Переписал на path-aware версию: сравнивает `new URL(link.href).pathname` с `window.location.pathname`, включает плавный скролл только если ссылка ведёт на ТЕКУЩУЮ страницу; если на другую — даёт браузеру перейти обычной навигацией, там якорь сработает нативно после загрузки. Если снова понадобится похожий трюк (общая шапка + якоря на секции одной конкретной страницы) — этот паттерн уже проверен, можно копировать.

## История (что было и куда делось)

```
0649b47  Главная страница сайта
14f4ebb  Создан README.md
78bde1a  Добавлена секция партнеры
c16f55e  Добавил в translations перевод   <- HEAD сейчас здесь
fbb8235  Казахская страница первой         <- origin/master всё ещё здесь
```

`origin/master` (GitHub) на `fbb8235` — на один коммит впереди локального `master`. Если запушить сейчас как есть — обычный `git push` откажет (не fast-forward), понадобится `git push --force`, если пользователь решит закрепить откат на GitHub. **Не пушить самостоятельно** без явной просьбы — это перезапишет удалённую историю.

**Что было построено поверх `fbb8235` в этой же сессии и затем дважды откачено пользователем целиком** (не как критика решений — пользователь прямо сказал: «результат меня не устроил, надо сначала заняться другими вещами»):

1. Первый заход: 4 отдельные HTML-страницы (`local-content.html`, `procurement.html`, `documents.html`, `news.html`) со своей копией шапки на каждой (шаблонизации в проекте нет — чистый HTML без сборки).
2. Второй заход (поверх первого): локальный бэкенд на **PocketBase** (SQLite внутри, встроенная админка на `/_/`, REST API) — 4 коллекции (`local_content`, `procurement`, `documents`, `news`), фронтенд подключён через `fetch` в `script.js`. Выбирали между PocketBase / Turso+свой API на Vercel / свой Express+SQLite — PocketBase выбрали за минимальную установку и встроенную админку. Хостили только локально (`127.0.0.1:8090`), на реальный хостинг сознательно не выкатывали.

Оба захода пользователь после этого попросил полностью откатить и удалить (включая саму папку `pocketbase/` с базой и процессом — я останавливал сервер `taskkill` и `rm -rf pocketbase`). **Ничего из этого сейчас не существует** — ни на диске, ни в git (это всё было либо не закоммичено, либо `pocketbase/` вообще не был частью git). Если понадобится похожий функционал снова — придётся строить заново, но **сначала уточнить у пользователя, какой подход в этот раз** (то, что не устроило в PocketBase-варианте, не зафиксировано — не знаю, что именно было не так: сам PocketBase, отдельные страницы, тайминг, или что-то ещё).

## Паттерн: пользователь откатывает работу через git/удаление файлов

За эту сессию это случалось трижды (включая один раз без предупреждения, приняв за баг, пока пользователь не объяснил, что это осознанно). Из этого практический вывод для меня:

- **Не считать несохранённую (незакоммиченную) работу надёжно сохранённой** между ответами — пользователь может откатить всё в любой момент, это его осознанный рабочий процесс («сделал — посмотрел — не понравилось — откатил — думаю дальше»), не случайность и не то, что нужно предотвращать.
- Когда пользователь просит «изучи проект заново» / файлы на диске не совпадают с тем, что я помню из истории чата — **доверять диску, не истории разговора**. Этот файл (`PROJECT_NOTES.md`) сам может быть удалён откатом — если его нет, это не ошибка, это сигнал перечитать всё заново.
- Не предлагать проактивно закоммитить «на всякий случай» без запроса — судя по поведению, пользователь коммитит осознанно и редко, крупными смысловыми кусками (см. историю коммитов — по одной завершённой фиче на коммит).

## Django-монолит (2026-09-24, по прямой просьбе: «подключи фронтенд в django через шаблонизатор, чтобы сайт был цельным монолитом»)

До этого: статические `index.html`/`styles.css`/`script.js` в корне `psa-landing/` + отдельный Django-бэкенд только под `/api/` и `/admin/`, фронтенд ходил за данными через `fetch()` (см. предыдущую задачу «подключи фронтенд» — это был промежуточный этап, теперь уже неактуальный).

Что сделал:
- `index.html` → `backend/templates/index.html` (Django-шаблон, `{% load static %}`, 4 раздела — `{% for entry in ...entries %}` вместо `<div id="...-list">` + JS fetch).
- `styles.css` → `backend/static/styles.css` (без изменений, просто перенос).
- `script.js` → `backend/static/script.js` — **вырезан весь блок «5. Контент с бэкенда»** (API_URL, loadApiCollection, createFeedItem и т.д., ~120 строк) — он больше не нужен, контент теперь рендерится сервером до отправки HTML браузеру. Всё остальное (i18n, мобильное меню, модалки проектов, валидация формы) — без изменений.
- `content/views.py`: добавлена `home(request)` — собирает все 4 queryset'а, рендерит `index.html` с контекстом. Старые `ReadOnlyModelViewSet` под `/api/` оставлены нетронутыми (уже не используются сайтом, но могут пригодиться внешним потребителям — см. `backend/README.md`).
- `psa_backend/urls.py`: `path('', home)` — корень теперь отдаёт страницу, не 404.
- `psa_backend/settings.py`: `TEMPLATES[0]['DIRS'] = [BASE_DIR/'templates']`, `STATICFILES_DIRS = [BASE_DIR/'static']`.
- **Старые `index.html`/`styles.css`/`script.js` в корне `psa-landing/` удалены** — сознательно, чтобы не было двух расходящихся копий фронтенда. Если увидишь, что они снова появились — это либо откат пользователем (см. «Паттерн» выше), либо кто-то намеренно вернул старую статическую версию; не мой косяк переноса.

Проверено (Playwright): страница рендерится, даты в правильном русском формате («01 января 2026 г.» — через Django template filter `|date:"d E Y"`, не JS `toLocaleDateString` как раньше), i18n-переключатель/мобильное меню/модалки проектов работают как прежде (их код не трогал), статика (`/static/styles.css`, `/static/script.js`) отдаётся, ошибок в консоли нет.

**Важное отличие от прежнего фронтенд-рендеринга:** раньше я вручную собирал DOM через `createElement`/`textContent`, чтобы обезопасить вставку данных с бэкенда (не доверять `innerHTML`). В Django-шаблонах это не нужно — `{{ entry.title }}` автоэкранируется движком по умолчанию, инъекция потребовала бы явного `|safe`, которого нигде не ставил.

## Разделение на отдельные страницы (2026-09-24, тем же днём, сразу после монолита)

Прямая просьба: «сделай так чтобы разделы... открывались в отдельной странице через меню. С главной страницы удали». Третий заход на эту же идею за сессию (см. «История» выше) — но впервые с нормальной шаблонизацией, поэтому сделал качественно иначе, не просто скопировал прошлый HTML-вариант:

- Ввёл `backend/templates/base.html` — шапка/меню/модалка проектов один раз, все 5 страниц через `{% extends %}`. Раньше (в откаченной HTML-версии) шапка была буквально скопирована в каждый файл — сам же отметил это в заметках как источник риска рассинхрона. Теперь это невозможно физически.
- 4 новые вьюхи в `content/views.py` (`local_content_page`, `procurement_page`, `documents_page`, `news_page`) + `home()` упрощена — больше не собирает 4 queryset'а, эти секции убраны со страницы.
- `psa_backend/urls.py`: 4 новых `path()`, `/local-content/`, `/procurement/`, `/documents/`, `/news/`.
- **script.js переписан для мульти-страничности**: раньше `a[href^="#"]` (чистый якорь) матчил только ссылки вида `#about`. Теперь шапка общая на все страницы и ссылки на разделы главной выглядят как `/#about` (абсолютный путь + якорь) — старый селектор бы их не поймал вообще, плавный скролл сломался бы молча. Переписал на `a[href*="#"]` + сравнение `new URL(link.href).pathname` с `location.pathname` — подробности в «Грабли» ниже.

Проверено (Playwright): на главной 4 раздела отсутствуют, все 5 URL отдают 200, плавный скролл на `/` работает для `#about` (реальный скролл, без перезагрузки), переход с `/local-content/` по ссылке `/#contacts` — обычная навигация + нативный якорь долетает до секции, данные из БД показываются на всех 4 страницах.

## Vercel (устарело с переходом на Django-монолит)

- Проект `psa-landing` в команде `zhomart`, `projectId`/`orgId` — в `.vercel/project.json` (гитигнорится, не в репозитории).
- Прод: https://psa-landing-gold.vercel.app — **больше не соответствует текущему репозиторию** (это была статика, Vercel Python/Django не хостит так просто). Файл всё ещё на диске, но неактуален как способ деплоя этого проекта.
- Для реального хостинга Django-версии нужен Python-совместимый PaaS (Railway/Render/Fly.io) или VPS — не решено, не начинал.

## Окружение (на будущее, если снова понадобится сеть/деплой)

Эта сеть/машина перехватывает HTTPS через государственный сертификат (Казахстан, «State Technical Service» / USIAG) — обычный `curl`/Node `fetch` не доверяют цепочке по умолчанию (`unable to get local issuer certificate`). Windows-сертификату доверяет, Node — нет, если не задать `NODE_OPTIONS=--use-system-ca`. `vercel login` нельзя выполнить через мой неинтерактивный Bash — только пользователь сам в своём терминале.

## Как я проверяю UI

`chromium-cli` в этом окружении недоступен. Паттерн: локальный сервер (раньше `python -m http.server <port>` для статики; теперь, с Django-монолитом, `manage.py runserver 127.0.0.1:8000`) + `npm install --no-save playwright@1.63.0` + `npx playwright install chromium` (один раз качает ~115MB) + инлайновые `node -e "import('playwright')..."` скрипты. **Важно:** `lsof -ti:PORT | xargs kill` в этом Git-Bash-on-Windows **не работает надёжно** — в одном случае оставил 6 висящих `python.exe` за несколько ответов подряд, каждый на своём порту. Правильный способ остановить процесс на Windows из этого Bash: найти PID через `netstat -ano | grep LISTENING`, проверить, что это ожидаемый процесс (`tasklist //FI "PID eq <pid>"`), затем `taskkill //F //PID <pid>`.

## Грабли, на которые уже наступал (актуально, если делать похожие вещи снова)

- **`[hidden]` перебивается CSS того же уровня специфичности**, если у элемента есть класс с `display:flex/block` — правило класса может победить (одинаковая специфичность, но позже в каскаде). Чинится явным `.selector[hidden] { display: none; }`.
- **`.parent ul` (descendant selector) ловит и вложенные `<ul>`** — например, дропдаун «Проекты» внутри `.main-nav` случайно наследовал `display:flex` от общего правила навигации. Использовать `.main-nav > ul` (прямой потомок) — этот фикс, кстати, единственный из «навигационных» уже закреплён в текущем состоянии (`c16f55e`), не откатился.
- **Hero был «кривым»** из-за двойного назначения класса `.container` (даёт `margin-inline:auto`) + свой `max-width` на одном и том же элементе — центрировался отдельно от остальных секций. Разделять: внешний `.container` для паддингов/центрирования, внутренний элемент — только ограничение ширины текста, без своего `margin:auto`.
- **`<title>`/`<meta description>` не подключены к i18n-системе** (`translate()`/`data-i18n` их не трогает) — при смене языка по умолчанию их обновлять вручную отдельно.
- **Плавный скролл по якорям ломается молча при переходе на общую шапку для нескольких страниц.** Селектор `a[href^="#"]` матчит только чистые якоря (`#about`); как только ссылки на разделы главной страницы стали `{% url 'home' %}#about` (т.е. реальный href `/#about`) в общем `base.html`, такие ссылки перестали матчиться вообще — не ошибка в консоли, просто плавный скролл тихо переставал работать на главной. Фикс: матчить `a[href*="#"]`, резолвить `new URL(link.href)` и сравнивать `.pathname` с `location.pathname` — включать плавный скролл только для ссылок на текущую страницу, иначе пропускать (браузер сам перейдёт и долетит до якоря нативно).
- **PocketBase-специфика (если снова возьмём этот бэкенд):** `editor`-поля хранят HTML из доверенного источника (только суперюзер через `/_/`) — их можно рендерить через `innerHTML` осознанно; для полей, куда потенциально сможет писать кто-то ещё (форма на сайте, не через админку) — только `textContent`. PocketBase не работает как SQLite-файл на Vercel serverless (нет постоянной ФС) — нужен отдельный постоянный хост с диском (или сам локальный запуск, как было здесь).

## Факты о компании (чтобы не выдумывать заново)

- ТОО «PSA» — Полномочный орган Правительства РК в СРП, учреждено **2010**, статус — по **Постановлению Правительства РК №355**, **100%** участие государства. Источник — прямо от пользователя, не проверял внешне.
- 3 проекта: **Кашаган** (Северо-Каспийский, шельф Каспия), **Карачаганак** (Западно-Казахстанская обл.), **Дунга** (Мангистауская обл.). Годы подписания соглашений и точные операторы для карточек-модалок не подтверждены документально.
- 7 партнёров (с https://psa.kz/partners/, через WebFetch, не перепроверял вручную): Минэнерго РК, АО «НК «КазМунайГаз»», NCOC (оператор Кашагана), KPO (оператор Карачаганака), Total E&P Dunga (оператор Дунги), НПП «Атамекен», KAZENERGY. Лого с чужого сайта не хотлинкать — карточки текстовые (осознанное решение).

## Дизайн-система (коротко, полное — в `styles.css` `:root`)

Цвета: `--ink-900 #0B2440`, `--steel-700 #1C4E80`, `--gold-500 #B8924B` (единственный акцент — один на экран), `--teal-600 #0E6E6B`, `--surface-0/100/900`, `--border-200`, `--text-secondary`, `--text-inverse`, `--state-success`.
Шрифты: PT Serif (заголовки) + PT Sans (текст) + PT Mono (реквизиты/даты) — выбраны из-за полной поддержки казахской кириллицы (ә ғ қ ң ө ұ ү h і).
Отступы кратны 4px (`--space-1..6`), радиусы `--radius-sm/md/lg/pill`.
